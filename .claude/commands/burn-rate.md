# USAGE — Claude Burn Rate Dashboard

Run and print the output verbatim. No commentary, no explanation, no formatting changes.

```bash
bash scripts/claude-usage.sh
```

If the output starts with `**ERROR:**`, stop — do not retry or troubleshoot.
